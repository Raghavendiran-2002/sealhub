name: SealHub PR Validation

on:
  pull_request:
    branches:
      - main
      - master

permissions:
  contents: write
  packages: write
  pull-requests: write

jobs:
  image_build_check:
    uses: vc-lsc/gha-workflow/.github/workflows/docker-buildx.yaml@v0.1
    with:
      docker-bake-path: ./docker-argo-bake.hcl
      push-image: false
    secrets: inherit
