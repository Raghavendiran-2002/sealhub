# Pi hub CLI — copied to ~/.config/sealhub/env on first install (not committed).
export SEALHUB_SERVER="http://127.0.0.1:8080"
export SEALHUB_TOKEN="pi-homelab-bootstrap-change-me"
export POCKET_ID_HOME="$HOME/pocket-id"
